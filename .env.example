# Anthropic
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6
CLAUDE_MAX_TOKENS=1024

# Where /analyse_risk POSTs the aggregated result after the agent finishes.
# Leave empty in dev to skip the callback.
CALLBACK_URL=
CALLBACK_TIMEOUT_SECONDS=10

# MongoDB — persistence + GET /analyses lookup
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=risk_control
MONGODB_COLLECTION=risk_analyses
